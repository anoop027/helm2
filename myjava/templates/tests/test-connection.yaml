apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myjava.fullname" . }}-test-connection"
  labels:
    {{- include "myjava.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myjava.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
